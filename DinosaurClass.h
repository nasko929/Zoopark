#ifndef __DINOSAUR_CLASS_H
#define __DINOSAUR_CLASS_H

/**
 * @brief An enum that keeps the types of dinosaur classes.
 */
enum DinosaurClass {
    CARNIVORE = 0,
    HERBIVORE,
    WATER_CLASS,
    FLYING_CLASS
};

#endif